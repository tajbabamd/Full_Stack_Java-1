
package TestCoding;

//import java.util.Arrays;
import java.util.Scanner;

public class PasswordConverter1 {

	public static void main(String[] args) {
//		TestPass2 t = new TestPass2("TestPass2");
//		public TestPass(String title) {
//			super();
//			
//	public PasswordCalculator1() {
			
		Scanner sc = new Scanner(System.in);
		System.out.println("Enter to convert:");
		String name = sc.nextLine();
		
		char [] a = name.toCharArray();		
		int [] e = new int[a.length];
		char [] f = new char[e.length];
		
		for(int i = 0; i < a.length; i++) {		
			int c = (int) a[i];
			int d;
			if(c%2 == 1) {
				d = c + 2;
			}else {
				d = c - 5;
			}
			e[i] = d;
			f[i] =(char) e[i];
		}
		
		String str = new String(f);
		System.out.println("String: " + str);
		
		System.out.println();	
		System.out.println();
			
		int j = 33, count = 0;
			
		for(char k = 33; k <= 126; k++) {	
			System.out.print("[" + (j++) + "] = [" + k + "], ");					
			count++;
			if(count == 5) {
				System.out.println();
				count = 0;
			}
		}

//	sc.close();
	}
		
}

